import { Repository } from 'typeorm';
import { Queue } from 'bull';
import { Resume } from './resume.entity';
import { ResumeFolder } from './resume-folder.entity';
import { CosService } from '../../common/storage/cos.service';
import { AiService } from './ai.service';
import { LocalParseService } from './local-parse.service';
import { DeduplicationService } from './deduplication.service';
import { ScoreService } from '../score/score.service';
import { RecommendationService } from '../recommendation/recommendation.service';
import { Talent } from '../talent/talent.entity';
export declare class ResumeService {
    private resumeRepository;
    private folderRepository;
    private talentRepository;
    private cosService;
    private resumeQueue;
    private emailQueue;
    private aiService;
    private localParseService;
    private deduplicationService;
    private scoreService;
    private recommendationService;
    private readonly logger;
    constructor(resumeRepository: Repository<Resume>, folderRepository: Repository<ResumeFolder>, talentRepository: Repository<Talent>, cosService: CosService, resumeQueue: Queue, emailQueue: Queue, aiService: AiService, localParseService: LocalParseService, deduplicationService: DeduplicationService, scoreService: ScoreService, recommendationService: RecommendationService);
    triggerEmailFetch(userId: string): Promise<void>;
    uploadResume(userId: string, file: any, folderId?: string): Promise<Resume>;
    private fastLocalParse;
    parseResumeAsync(resumeId: string): Promise<void>;
    private calculateScoreAndTier;
    private mapAiResultToTalent;
    private extractTextFromFile;
    private getFileType;
    private generateTags;
    getResumes(userId: string, folderId?: string, searchParams?: any): Promise<Resume[]>;
    getResumeById(id: string, userId: string): Promise<Resume>;
    createFolder(userId: string, name: string, parentId?: string): Promise<ResumeFolder>;
    getFolders(userId: string): Promise<ResumeFolder[]>;
    private getUserBehavior;
    deleteResume(id: string, userId: string): Promise<void>;
}
