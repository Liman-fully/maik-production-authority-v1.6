# 🚨 WAR-ROOM UPDATE: 20:19. OPTIMIZED FOR EARNINGS & M1 POWER.

## 👤 致 Workbuddy (唯一物理执行端)
你现在的战位是: 利用本地 M1 (8G RAM) 性能优势，对冲远端 2G 服务器的贫瘠。

## 🏹 本阶段最高商业指令: “先赚钱，再扩容”
1. **杠杆部署**: 利用本地 Docker 跑通 NestJS+Redis。不要在那台弱服务器上产生构建负载。
2. **UI 强制复活**: 寻找并物理搬运 M1 构建出的 dist 到 Nginx 根目录。今晚 199 必须展现“职场社交版”。
3. **去歧视算法**: 判定人才职级采用 [薪资/公司/年限/职级] 优势包络算法。物理隐藏年龄字段。

## 🚨 必须遵守的准则
- 严禁删除 docs/ 下的秘密映射文件。
- 同步周期: 每次重大优化后 push 到云端 [huntlink-new]。
- 验收标准: 刷新 199 页面看到“职场人脉”数值而非“简历数”。

**你的“决策大脑”Nova 已经完成逻辑闭环。现在请开始你的物理表演。**

## 🚨 22:12 审美与数据紧急加固 (Hotpatch)
- **UI BUG**: 导航栏已由 Nova 完成源码级修复（注入了 justify-between）。请立即重新编译部署！
- **DATA CRITICAL**: 199 页面目前是一片荒漠。立即进入 backend 目录，手动执行 `node dist/modules/seed/main.js` 或类似的 Mock 数据脚本。我要看到至少 10 个活生生的人才卡片！

## 📏 响应式布局铁律 (Deterministic UI)
- **居中锚定**: 所有的容器必须携带 `mx-auto max-w-[1440px]`。严禁让内容在大屏下随意贴边。
- **降维收缩**: 屏幕变窄时，优先减小间距 (`gap`)，最后才允许元素掉队 (`flex-wrap`)。
- **稳定性优先**: 导航栏高度强制固定 (`h-16`)，严禁在不同尺寸下产生高度漂移。

## 💓 10分钟心跳同步协议 (Autonomous Pulse)
- 每 10 分钟，你必须重新读取 `.nova_sync/logic_pulse.json`。
- 那里是 Nova (大脑) 下发的纯逻辑补丁。直接执行物理应用 (`patch` 或 `sed`)。
- 严禁因为简单的文件覆盖向 Liman 发起确认。只有在环境物理损坏时才允许停下。

## 📏 物理目录契约 (File Structure Constraint)
- **强制锚定**: 如果你想移动文件，必须首先核对 `docs/STRUCTURE_MANIFEST.md`。
- **全站扫描**: 严禁因为目录移动产生 404。每次变动必须执行 import 全量审计。

## 🛡️ 简历渲染自愈 (UI Robustness)
- **拒绝坍塌**: 严禁因为简历解析数据缺失导致 TalentCard 产生位移。
- **强制对齐**: 必须使用 `line-clamp` 属性固定文本行数。
- **自愈机制**: 后端解析失败的字段，前端必须显示由 Nova 定义的优雅占位符。

## 🎨 职场名片第一印象 (Impact Card)
- **视觉分级**: 点击头像弹出的名片必须携带 RankScore 驱动的背景渐变。
- **信息遮蔽**: 真实联系方式严禁在未支付/未交换前呈现，必须使用高斯模糊 (backdrop-blur)。
- **审美闭环**: 必须包含 3 个由 Nova 算法生成的“势能胶囊”标签，提升人才溢价。
